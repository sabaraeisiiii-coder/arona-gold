import { StoreShell } from '../components/StoreShell';import { ProductGrid } from '../components/ProductGrid';import { searchProducts } from '../services/product.service';
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string}>}){const{q=''}=await searchParams;const result=await searchProducts(q);return <StoreShell><section className="page"><header className="page-head"><span className="eyebrow">پیدا کردن محصول</span><h1>جستجو</h1><p>نام، دسته‌بندی یا کد محصول را وارد کنید.</p></header><form action="/search" role="search"><input autoFocus className="ds-input search-input" name="q" defaultValue={q} placeholder="جستجوی محصول یا کد محصول"/></form>{q&&(result.length?<><p className="result-count">{result.length.toLocaleString('fa-IR')} نتیجه برای «{q}»</p><ProductGrid products={result}/></>:<div className="ds-card empty"><b>⌕</b><h2>نتیجه‌ای پیدا نشد</h2><p>عبارت دیگری را امتحان کنید.</p></div>)}</section></StoreShell>}
