import type { NextRequest } from 'next/server'; import { apiSuccess } from '@/app/lib/api-response'; import { withApiHandler } from '@/app/lib/api-handler'; import { validateJsonBody } from '@/app/validation/request'; import { verifyOtpSchema } from '@/app/auth/schema'; import { AUTH_COOKIE,AuthService } from '@/app/auth/service'; import { requestIdentity } from '@/app/auth/request'; import { secureCookieOptions } from '@/app/security/cookies'; import { getConfig } from '@/app/config/env';
export const POST=withApiHandler(async(request,{requestId,logger})=>{const input=await validateJsonBody(request,verifyOtpSchema);const result=await new AuthService().verifyOtp(input.mobile,input.code,requestIdentity(request),logger);const response=apiSuccess({user:result.user},{requestId});response.cookies.set(AUTH_COOKIE,result.token,{...secureCookieOptions(getConfig().APP_ENV),maxAge:result.expiresIn});return response;});
export type _NextRequestCookieCompatibility = NextRequest;
