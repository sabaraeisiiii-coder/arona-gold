import type { NextRequest } from 'next/server';import { apiSuccess } from '@/app/lib/api-response';import { withApiHandler } from '@/app/lib/api-handler';import { requireAuth } from '@/app/auth/request';import { validateParams } from '@/app/validation/request';import { addressParamsSchema } from '@/app/addresses/schema';import { AddressService } from '@/app/addresses/service';
type Context={params:Promise<{id:string}>};export async function POST(request:NextRequest,context:Context){return withApiHandler(async(_req,{requestId})=>{const user=await requireAuth(request);const{id}=validateParams(await context.params,addressParamsSchema);return apiSuccess(await new AddressService().setDefault(user.id,id),{requestId});})(request)}
