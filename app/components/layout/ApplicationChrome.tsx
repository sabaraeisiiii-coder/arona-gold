'use client';
import { usePathname } from 'next/navigation';import type { ReactNode } from 'react';import { Header } from './Header';import { Footer } from './Footer';import { Container } from './Container';import { AccountLayout } from '../account/AccountLayout';
export function ApplicationChrome({children}:{children:ReactNode}){const pathname=usePathname();if(pathname.startsWith('/admin'))return <>{children}</>;return <div className="site-shell"><Header/><main><Container className="container">{['/profile','/orders','/addresses','/wishlist','/notifications'].some(route=>pathname===route||pathname.startsWith(route+'/'))?<AccountLayout>{children}</AccountLayout>:children}</Container></main><Footer/></div>}
