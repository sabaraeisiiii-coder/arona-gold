import type { ReactNode } from 'react';
/** Transitional wrapper retained while route files are incrementally simplified. Navigation is owned by app/layout.tsx. */
export function StoreShell({children}:{children:ReactNode}){return <>{children}</>}
