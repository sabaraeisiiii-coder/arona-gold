import Link from 'next/link';import { StatusBadge } from '../admin/StatusBadge';import { formatMoney } from '@/app/lib/format';import type { Order } from '@/app/store/AppStore';export function OrderCard({order}:{order:Order}){return <Link className="ds-card order-card" href={`/orders/${order.id}`}><div><strong>{order.id}</strong><small>{order.date}</small></div><StatusBadge status={order.status}/><b>{formatMoney(order.amount)}</b></Link>}
