import type { ApiAddress } from '@/app/services/address.service';import { Badge } from '../ui/Badge';import { Button } from '../ui/Button';export function AddressCard({address,onEdit,onDelete,onDefault,pending=false}:{pending?:boolean;address:ApiAddress;onEdit:()=>void;onDelete:()=>void;onDefault:()=>void}){return <article className="ds-card address-card">{address.isDefault&&<Badge variant="info">پیش‌فرض</Badge>}<h3>{address.receiverName}</h3><p>{address.province}، {address.city}، {address.address}</p><small>{address.mobile} — {address.postalCode}</small><div className="inline-actions"><Button variant="secondary" disabled={pending} onClick={onEdit}>ویرایش</Button>{!address.isDefault&&<Button variant="ghost" disabled={pending} onClick={onDefault}>پیش‌فرض</Button>}<Button variant="danger" disabled={pending} onClick={onDelete}>حذف</Button></div></article>}
