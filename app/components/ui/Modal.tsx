'use client';
import { useEffect,useId,useRef,type ReactNode } from 'react';import { Button } from './Button';
export function Modal({open,title,description,children,footer,onClose,closeOnBackdrop=true}:{open:boolean;title:string;description?:string;children:ReactNode;footer?:ReactNode;onClose:()=>void;closeOnBackdrop?:boolean}){const titleId=useId(),descriptionId=useId(),dialog=useRef<HTMLElement>(null);useEffect(()=>{if(!open)return;const previous=document.activeElement as HTMLElement|null;dialog.current?.focus();const key=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose()};document.addEventListener('keydown',key);return()=>{document.removeEventListener('keydown',key);previous?.focus()}},[open,onClose]);if(!open)return null;return <div className="ds-modal-backdrop" onMouseDown={()=>closeOnBackdrop&&onClose()}><section ref={dialog} tabIndex={-1} className="ds-modal" role="dialog" aria-modal="true" aria-labelledby={titleId} aria-describedby={description?descriptionId:undefined} onMouseDown={e=>e.stopPropagation()}><header className="modal-head"><div><h2 id={titleId}>{title}</h2>{description&&<p id={descriptionId}>{description}</p>}</div><Button variant="ghost" size="sm" aria-label="بستن" onClick={onClose}>×</Button></header><div className="ds-modal-content">{children}</div>{footer&&<footer className="ds-modal-footer">{footer}</footer>}</section></div>}
