import type { ReactNode } from 'react';export function EmptyState({icon,title,description,action,className=''}:{icon?:ReactNode;title:string;description?:string;action?:ReactNode;className?:string}){return <div className={`ds-card empty ds-empty-state ${className}`.trim()}>{icon&&<div className="ds-empty-icon" aria-hidden="true">{icon}</div>}<h2>{title}</h2>{description&&<p>{description}</p>}{action}</div>}
