import { useId,type SelectHTMLAttributes } from 'react';
export function Select({label,error,hint,children,id,className='',...props}:SelectHTMLAttributes<HTMLSelectElement>&{label:string;error?:string;hint?:string}){const generated=useId(),fieldId=id??generated;return <label className="ds-field" htmlFor={fieldId}><span className="ds-label">{label}</span><select id={fieldId} className={`ds-input ${className}`.trim()} aria-invalid={Boolean(error)||undefined} {...props}>{children}</select>{(error||hint)&&<small className={error?'ds-field-error':'ds-field-hint'}>{error??hint}</small>}</label>}
