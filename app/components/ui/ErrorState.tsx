import { Button } from './Button';export function ErrorState({title='خطایی رخ داد',description,retry,errorCode}:{title?:string;description:string;retry?:()=>void;errorCode?:string}){return <div className="ds-card empty ds-error-state" role="alert"><h2>{title}</h2><p>{description}</p>{errorCode&&<code>{errorCode}</code>}{retry&&<Button variant="outline" onClick={retry}>تلاش دوباره</Button>}</div>}
