import { Modal } from '../ui/Modal';import { Button } from '../ui/Button';export function ConfirmDialog({open,title,description,onConfirm,onClose,loading=false}:{open:boolean;title:string;description:string;onConfirm:()=>void;onClose:()=>void;loading?:boolean}){return <Modal open={open} title={title} description={description} onClose={onClose} footer={<><Button variant="secondary" onClick={onClose}>انصراف</Button><Button variant="danger" loading={loading} onClick={onConfirm}>تأیید</Button></>}>{null}</Modal>}
