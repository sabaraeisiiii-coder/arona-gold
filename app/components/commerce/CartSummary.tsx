import type { ReactNode } from 'react';export function CartSummary({subtotal,discount=0,action}:{subtotal:number;discount?:number;action?:ReactNode}){return <aside className="ds-card order-summary"><h2>خلاصه سبد</h2><p>جمع کالاها <strong>{subtotal.toLocaleString('fa-IR')} تومان</strong></p>{discount>0&&<p>تخفیف <strong>-{discount.toLocaleString('fa-IR')} تومان</strong></p>}<p>قابل پرداخت <strong>{(subtotal-discount).toLocaleString('fa-IR')} تومان</strong></p>{action}</aside>}
