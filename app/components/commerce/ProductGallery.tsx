'use client';
import { useState } from 'react';import type { Product } from '../../types/product';import { getPrimaryProductImage } from '../../lib/product-image';import { ProductMedia } from './ProductMedia';
export function ProductGallery({product}:{product:Product}){const primary=getPrimaryProductImage(product);const[selectedSrc,setSelectedSrc]=useState(primary?.src);const selected=product.images.find(image=>image.src===selectedSrc)??primary;return <div className="product-gallery"><ProductMedia product={product} image={selected} className="product-gallery__main" sizes="(max-width: 767px) 100vw, 50vw" priority/>{product.images.length>1&&<div className="product-gallery__thumbnails" aria-label="تصاویر محصول">{product.images.map(image=><button type="button" key={image.src} className={image.src===selected?.src?'active':''} onClick={()=>setSelectedSrc(image.src)} aria-label={`نمایش ${image.alt}`} aria-pressed={image.src===selected?.src}><ProductMedia product={product} image={image} sizes="96px"/></button>)}</div>}</div>}
