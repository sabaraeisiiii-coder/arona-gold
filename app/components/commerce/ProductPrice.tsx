import { formatMoney } from '@/app/lib/format';export function ProductPrice({price,oldPrice,discountPercent,className=''}:{price:number;oldPrice?:number;discountPercent?:number;className?:string}){return <span className={`product-price ${className}`.trim()}><data className="product-card__price" value={price}>{formatMoney(price)}</data>{oldPrice&&oldPrice>price&&<del>{formatMoney(oldPrice)}</del>}{discountPercent&&<span className="product-discount">٪{discountPercent.toLocaleString('fa-IR')}</span>}</span>}
